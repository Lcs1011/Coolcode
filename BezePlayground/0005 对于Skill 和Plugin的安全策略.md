

## 对于

那么我们的安全策略就是如下的说法。 我就采用这个策略，可以ba ?
### 可以放行的是：

```
官方源码中“已经看得见、已经审计过、已经接入 safe_network / safe_fs 门禁”的 plugin 宿主代码。
```

也就是：

```
plugin 系统里的本地框架代码可以保留。
```

前提是它所有网络、读写、创建、删除都走我们的门禁。

### 必须关闭的是：

```
远程下载 plugin解压远程 plugin安装远程 plugin自动同步 plugin自动扫描外部 plugin store自动加载未知 plugin
```

也就是说

```
保留 plugin 宿主框架。
关闭 plugin 远程安装生态。
只允许本地 已经 被门禁管理的改造过的 白名单 plugin。
```