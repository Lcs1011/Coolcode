
## 傻瓜指令

修改文件：

```text
C:\CodexLab\codex\codex-rs\config\src\thread_config\remote.rs
```

找到：

```rust
        auth: provider
            .auth
            .map(model_provider_auth_from_proto)
            .transpose()?,
        aws: None,
        wire_api,
```

改成：

```rust
        auth: provider
            .auth
            .map(model_provider_auth_from_proto)
            .transpose()?,
        wire_api,
```

修改文件：

```text
C:\CodexLab\codex\codex-rs\config\src\thread_config\remote.rs
```

找到：

```rust
        experimental_bearer_token,
        auth,
        aws: _,
        wire_api,
```

改成：

```rust
        experimental_bearer_token,
        auth,
        wire_api,
```

修改文件：

```text
C:\CodexLab\codex\codex-rs\config\src\thread_config\remote.rs
```

找到：

```rust
        websocket_connect_timeout_ms: Some(10_000),
        requires_openai_auth: false,
        supports_websockets: true,
        aws: None,
    }
```

改成：

```rust
        websocket_connect_timeout_ms: Some(10_000),
        requires_openai_auth: false,
        supports_websockets: true,
    }
```