
## 傻瓜指令

修改文件：

```text
C:\CodexLab\codex\codex-rs\Cargo.toml
```

找到：

```toml
"aws-auth",
```

删除这一段。

在同一个文件里找到：

```toml
codex-aws-auth = { path = "aws-auth" }
```

删除这一段。

在同一个文件里找到：

```toml
aws-config = "1"
```

删除这一段。

在同一个文件里找到：

```toml
aws-credential-types = "1"
```

删除这一段。

在同一个文件里找到：

```toml
aws-sigv4 = "1"
```

删除这一段。

在同一个文件里找到：

```toml
aws-types = "1"
```

删除这一段。

修改文件：

```text
C:\CodexLab\codex\codex-rs\model-provider\Cargo.toml
```

找到：

```toml
codex-aws-auth = { workspace = true }
```

删除这一段。

删除整个文件夹：

```text
C:\CodexLab\codex\codex-rs\aws-auth
```

删除整个文件夹：

```text
C:\CodexLab\codex\codex-rs\model-provider\src\amazon_bedrock
```

修改文件：

```text
C:\CodexLab\codex\codex-rs\model-provider\src\lib.rs
```

找到：

```rust
mod amazon_bedrock;
```

删除这一段。

修改文件：

```text
C:\CodexLab\codex\codex-rs\model-provider\src\provider.rs
```

找到：

```rust
use crate::amazon_bedrock::AmazonBedrockModelProvider;
```

删除这一段。

在同一个文件里找到这个函数：

```rust
pub fn create_model_provider(
    provider_info: ModelProviderInfo,
    auth_manager: Option<Arc<AuthManager>>,
) -> SharedModelProvider {
    if provider_info.is_amazon_bedrock() {
        Arc::new(AmazonBedrockModelProvider::new(provider_info))
    } else {
        Arc::new(ConfiguredModelProvider::new(provider_info, auth_manager))
    }
}
```

把它改成：

```rust
pub fn create_model_provider(
    provider_info: ModelProviderInfo,
    auth_manager: Option<Arc<AuthManager>>,
) -> SharedModelProvider {
    Arc::new(ConfiguredModelProvider::new(provider_info, auth_manager))
}
```

修改文件：

```text
C:\CodexLab\codex\codex-rs\model-provider-info\src\lib.rs
```

找到：

```rust
const AMAZON_BEDROCK_PROVIDER_NAME: &str = "Amazon Bedrock";
```

删除这一行。

找到：

```rust
pub const AMAZON_BEDROCK_PROVIDER_ID: &str = "amazon-bedrock";
```

删除这一行。

找到：

```rust
pub const AMAZON_BEDROCK_GPT_5_5_MODEL_ID: &str = "openai.gpt-5.5";
```

删除这一行。

找到：

```rust
pub const AMAZON_BEDROCK_GPT_5_4_MODEL_ID: &str = "openai.gpt-5.4";
```

删除这一行。

找到：

```rust
pub const AMAZON_BEDROCK_DEFAULT_BASE_URL: &str = "https://bedrock-mantle.us-east-1.api.aws/openai/v1";
```

删除这一行。

找到：

```rust
const AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_HEADER: &str = "x-amzn-mantle-client-agent";
```

删除这一行。

找到：

```rust
const AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_VALUE: &str = "codex";
```

删除这一行。

在同一个文件里找到 `ModelProviderInfo` 结构体里面这一段：

```rust
pub aws: Option<ModelProviderAwsAuthInfo>,
```

删除这一段。

在同一个文件里找到整个结构体：

```rust
#[derive(Debug, Clone, Deserialize, Serialize, PartialEq, Eq, JsonSchema)]
#[schemars(deny_unknown_fields)]
pub struct ModelProviderAwsAuthInfo {
    /// AWS profile name to use. When unset, the AWS SDK default chain decides.
    pub profile: Option<String>,
    /// AWS region to use for provider-specific endpoints.
    pub region: Option<String>,
}
```

删除整个结构体。

在同一个文件里找到 `impl ModelProviderInfo` 里的 `validate` 函数。

在 `validate` 函数开头找到这一整段：

```rust
if self.aws.is_some() {
    if self.supports_websockets {
        return Err("provider aws cannot be combined with supports_websockets".to_string());
    }
    let mut conflicts = Vec::new();
    if self.env_key.is_some() {
        conflicts.push("env_key");
    }
    if self.experimental_bearer_token.is_some() {
        conflicts.push("experimental_bearer_token");
    }
    if self.auth.is_some() {
        conflicts.push("auth");
    }
    if self.requires_openai_auth {
        conflicts.push("requires_openai_auth");
    }
    if !conflicts.is_empty() {
        return Err(format!(
            "provider aws cannot be combined with {}",
            conflicts.join(", ")
        ));
    }
}
```

删除这一整段。

在同一个文件里找到 `create_openai_provider` 函数里的这一段：

```rust
aws: None,
```

删除这一段。

在同一个文件里找到整个函数：

```rust
pub fn create_amazon_bedrock_provider(
    aws: Option<ModelProviderAwsAuthInfo>,
) -> ModelProviderInfo {
    ModelProviderInfo {
        name: AMAZON_BEDROCK_PROVIDER_NAME.into(),
        base_url: Some(AMAZON_BEDROCK_DEFAULT_BASE_URL.into()),
        env_key: None,
        env_key_instructions: None,
        experimental_bearer_token: None,
        auth: None,
        aws: Some(aws.unwrap_or(ModelProviderAwsAuthInfo {
            profile: None,
            region: None,
        })),
        wire_api: WireApi::Responses,
        query_params: None,
        http_headers: Some(HashMap::from([(
            AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_HEADER.to_string(),
            AMAZON_BEDROCK_MANTLE_CLIENT_AGENT_VALUE.to_string(),
        )])),
        env_http_headers: None,
        request_max_retries: None,
        stream_max_retries: None,
        stream_idle_timeout_ms: None,
        websocket_connect_timeout_ms: None,
        requires_openai_auth: false,
        supports_websockets: false,
    }
}
```

删除整个函数。

在同一个文件里找到：

```rust
pub fn is_amazon_bedrock(&self) -> bool {
    self.name == AMAZON_BEDROCK_PROVIDER_NAME
}
```

删除整个函数。

在同一个文件里找到 `built_in_model_providers` 函数里面这一行：

```rust
let amazon_bedrock_provider = P::create_amazon_bedrock_provider(/*aws*/ None);
```

删除这一行。

在同一个文件里找到 `built_in_model_providers` 函数里面这一段：

```rust
(AMAZON_BEDROCK_PROVIDER_ID, amazon_bedrock_provider),
```

删除这一段。

在同一个文件里找到 `merge_configured_model_providers` 函数。

把整个函数：

```rust
pub fn merge_configured_model_providers(
    mut model_providers: HashMap<String, ModelProviderInfo>,
    configured_model_providers: HashMap<String, ModelProviderInfo>,
) -> Result<HashMap<String, ModelProviderInfo>, String> {
    for (key, mut provider) in configured_model_providers {
        if key == AMAZON_BEDROCK_PROVIDER_ID {
            let aws_override = provider.aws.take();
            if provider != ModelProviderInfo::default() {
                return Err(format!(
                    "model_providers.{AMAZON_BEDROCK_PROVIDER_ID} only supports changing \
                     `aws.profile` and `aws.region`; other non-default provider fields are not supported"
                ));
            }
            if let Some(aws_override) = aws_override
                && let Some(built_in_provider) = model_providers.get_mut(AMAZON_BEDROCK_PROVIDER_ID)
                && let Some(built_in_aws) = built_in_provider.aws.as_mut()
            {
                if let Some(profile) = aws_override.profile {
                    built_in_aws.profile = Some(profile);
                }
                if let Some(region) = aws_override.region {
                    built_in_aws.region = Some(region);
                }
            }
        } else {
            model_providers.entry(key).or_insert(provider);
        }
    }
    Ok(model_providers)
}
```

改成：

```rust
pub fn merge_configured_model_providers(
    mut model_providers: HashMap<String, ModelProviderInfo>,
    configured_model_providers: HashMap<String, ModelProviderInfo>,
) -> Result<HashMap<String, ModelProviderInfo>, String> {
    for (key, provider) in configured_model_providers {
        model_providers.entry(key).or_insert(provider);
    }
    Ok(model_providers)
}
```

在同一个文件里找到 `create_oss_provider_with_base_url` 函数里的这一段：

```rust
aws: None,
```

删除这一段。

修改文件：

```text
C:\CodexLab\codex\codex-rs\model-provider-info\src\model_provider_info_tests.rs
```

删除文件里所有这一行：

```rust
aws: None,
```

删除整个测试函数：

```rust
fn test_deserialize_provider_aws_config()
```

删除整个测试函数：

```rust
fn test_create_amazon_bedrock_provider()
```

删除整个测试函数：

```rust
fn test_amazon_bedrock_provider_adds_mantle_client_agent_header()
```

删除整个测试函数：

```rust
fn test_built_in_model_providers_include_amazon_bedrock()
```

删除整个测试函数：

```rust
fn test_merge_configured_model_providers_applies_amazon_bedrock_profile_override()
```

删除整个测试函数：

```rust
fn test_merge_configured_model_providers_rejects_amazon_bedrock_non_default_fields()
```

删除整个测试函数：

```rust
fn test_merge_configured_model_providers_allows_amazon_bedrock_default_fields()
```

删除整个测试函数：

```rust
fn test_validate_provider_aws_rejects_conflicting_auth()
```

删除整个测试函数：

```rust
fn test_validate_provider_aws_rejects_websockets()
```

修改文件：

```text
C:\CodexLab\codex\codex-rs\protocol\src\account.rs
```

找到：

```rust
pub enum ProviderAccount {
    ApiKey,
    Chatgpt { email: String, plan_type: PlanType },
    AmazonBedrock,
}
```

把它改成：

```rust
pub enum ProviderAccount {
    ApiKey,
    Chatgpt { email: String, plan_type: PlanType },
}
```

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
AmazonBedrock
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
Amazon Bedrock
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
amazon-bedrock
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
AMAZON_BEDROCK
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
ModelProviderAwsAuthInfo
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
codex_aws_auth
```

删除所有剩余命中。

在整个文件夹：

```text
C:\CodexLab\codex\codex-rs
```

里面搜索：

```text
codex-aws-auth
```

删除所有剩余命中。