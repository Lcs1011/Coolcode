你现在只执行我接下来给你的“傻瓜指令”。

执行规则：

1. 只修改傻瓜指令里明确写出的文件。
2. 只删除或替换傻瓜指令里明确写出的代码块。
3. 不要全仓库自由重构。
4. 不要搜索到额外命中后自行删除。
5. 不要修改没有被傻瓜指令点名的文件。
6. 不要自行修复测试、schema、snapshot、文档或其他残留。
7. 如果某个“原来的代码块”找不到，立刻停止，并告诉我找不到哪个文件里的哪一段。
8. 如果发现还有其他相关引用，不要处理，只列出来。
9. 执行完成后，只汇报：

   * 修改了哪些文件
   * 删除了哪些文件夹
   * 哪些指令没有成功执行
   * 还有哪些未处理命中
10. 不要继续下一阶段，不要追加计划，不要自动运行长时间任务。
