

## 第一步，创建 Safe Mode
默认值为 On
创建枚举变量
网络访问和接收 目的 枚举变量

对文件和文件夹读、写、增、删  目的 枚举变量

然后用我们的函数包住所有的原生的
网络访问和接收函数
对文件和文件夹读、写、增、删 函数
每一个函数的调用位置都硬编码上，目的枚举变量。

然后有一个批准函数。 在 Safe Mode 开启的情况下。 会阻挡目的变量不符合要求的函数。




## 第二步
添加 PromisingProfile  叫 CoolReadWrite
设置 CoolReadWrite 为 CLI启动的 默认 PromisingProfile
不给 PromisingProfile 任何 工具


## 第三步
创建CTool 视野系统  

创建视野枚举/
None
CoolWorkspace
SelectedOnly
TheEyeofProvidence
默认为None


## 第四步 最开头显示的状态包括：

SafeMode 状态
PermissionProfile 状态
CToolScope 状态
CoolWorkspace 路径

## 第五步
陆续添加CTool给 CoolReadWrite
或者审核通过 部分原生工具 给 CoolReadWrite




  just fmt
  just test -p codex-tui

  如果 ctool 还没测，也再跑：

  just test -p ctool